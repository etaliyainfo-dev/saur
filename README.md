# Elementor Template Export

This repository contains an Elementor JSON template that wraps the provided landing page HTML inside a single HTML widget. Import the JSON file in WordPress (Templates → Saved Templates → Import Templates) and then edit the HTML widget to update checkout links or copy tweaks.

## Files
- `elementor-template.json` – Elementor page template with the full HTML, Tailwind CDN, and inline scripts.
